import { Product } from "@/sanity.types";
import React from "react";

const ProductSideMenu = ({
  product,
  className,
}: {
  product: Product;
  className?: string;
}) => {
  return <div>ProductSideMenu</div>;
};

export default ProductSideMenu;
